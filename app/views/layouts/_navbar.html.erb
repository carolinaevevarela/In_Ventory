<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" href="#">InVentory</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <a class="nav-link" href="<%= pages_index_path %>">Home <span class="sr-only">(current)</span></a>
      </li>
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          <% if user_signed_in? %>
            Bienvenido, <%= current_user.email.partition('@').first %>
          <% else %>
            Ingreso
          <% end %>
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
          <% if user_signed_in? %>
            <a class="dropdown-item" href="<%= edit_user_registration_path%>">Editar usuario</a>
            <a class="dropdown-item" href="<%= destroy_user_session_path%>" data-method="DELETE">Salir</a>
          <% else %>
            <a class="dropdown-item" href="<%= new_user_session_path%>">Ingreso</a>
            <a class="dropdown-item" href="<%= new_user_registration_path%>">Registro</a>
          <% end %>
        </div>
      </li>
      <% if user_signed_in? %>
      <li class="nav-item active">
        <a class="nav-link" href="<%= new_book_path%>">Crear libro</a>
      </li>
      <% end %>
      <% if user_signed_in? %>
      <li class="nav-item active">
        <a class="nav-link" href="#">Ingreso guía</a>
      </li>
      <% end %>
      <span class="navbar-text"><li><%= current_user.last_sign_in_at if current_user.present? %></li></span>
    </ul>
  </div>
</nav>
